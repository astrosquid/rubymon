require 'rubymon'

describe 'Rubymon' do

  puts "No tests for Class Rubymon at this time"

  # describe '::new' do
  #   it 'get initialized with an id, species, level, and types' do
  #     # expect{Book.new("And Then There Were None")}.to_not raise_error
  #     expect{Rubymon.new(1, 'Bulbasaur', 5, ["Grass"])}.to_not raise_error
  #   end
  # end
  #
  # describe 'properties' do
  #   it 'does not allow types to be mutable' do
  #     bulba = Rubymon.new(1, 'Bulbasaur', 5, ["Grass"])
  #     expect{bulba.types = ["Fire"]}.to raise_error
  #   end
  # end


end
